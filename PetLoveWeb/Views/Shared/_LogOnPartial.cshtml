@if(Request.IsAuthenticated) {
    <text>Bem-Vindo(a) <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Meus Dados", "ChangePassword", "Account") ]
    [ @Html.ActionLink("Sair", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Cadastrar-se", "Register", "Account") ]
    @:[ @Html.ActionLink("Entrar", "LogOn", "Account") ]
}
